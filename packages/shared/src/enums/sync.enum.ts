export enum ESyncEntity {
  PATIENT = 'patient',
  EXAMINATION = 'examination',
  TEST = 'test',
}

export enum ESyncOperation {
  CREATE = 'create',
  UPDATE = 'update',
  DELETE = 'delete',
}

export enum EIdType {
  TEMP = 'temp',
  PERMANENT = 'permanent',
}
